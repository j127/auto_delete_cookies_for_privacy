# Firefox manual test matrix

The pre-merge gate for the Firefox port (issue #290): run every row once per browser channel on a real, headed Firefox before `add-firefox-support` merges to `main`, record the outcome in the Result columns, and file a follow-up issue for every failure (reference it in the row). Automated coverage notes say what the unit/regression suites already pin so the manual pass can focus on what only a real browser shows.

Automated companion: `just e2e_firefox` drives a real headless Firefox through rows 2, 3, 5 (and the row-13 plumbing) with a privileged extension-page probe — run it before starting a manual pass; the rows it covers then only need spot-checking. Setup for every session: `just build_firefox`, then either `just run_firefox` (throwaway profile) or `about:debugging#/runtime/this-firefox` → "Load Temporary Add-on" → any file inside `builds/firefox/`. Use a test site that sets cookies + localStorage (e.g. an httpbin cookie page plus any site with a consent banner), and keep the extension's Debug Mode on so the background console narrates decisions.

Channels: **ESR** = Firefox ESR 128, **Rel** = current Firefox release.

| #   | Scenario                       | Steps                                                                                                                          | Expected                                                                                                                     | Automated coverage                                                                                                                                                                                                     | ESR | Rel |
| --- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | --- |
| 1   | Temporary install              | Load the artifact via about:debugging; open popup + settings                                                                   | Loads with no manifest warnings beyond the known data_collection min-version notice; popup and settings render               | web-ext lint 0 errors in CI                                                                                                                                                                                            |     |     |
| 2   | Baseline cleanup (TCP default) | Visit test site, close its tab, wait past the clean delay with Automatic Cleaning on                                           | Site cookies removed on the tab-close cleanup; activity log shows the domain                                                 | Gecko-jar regression suite (bugs 1–3); e2e row 2 (`just e2e_firefox`)                                                                                                                                                  |     |     |
| 3   | Partitioned tracker (TCP)      | Visit a site embedding third-party widgets (default TCP partitions them); close the tab                                        | Partitioned third-party cookies for that top-level site are gone; popup count included them while open                       | bug 2 pin; popup count spec; e2e row 3 (real TCP partition round-trip)                                                                                                                                                 |     |     |
| 4   | ETP strict                     | Set ETP to Strict, repeat row 2                                                                                                | Same outcome as row 2                                                                                                        | — (config variant only)                                                                                                                                                                                                |     |     |
| 5   | FPI profile                    | Fresh profile with `privacy.firstparty.isolate=true`; repeat row 2; also verify the marker cookie appears (site-data types on) | Cleanup works; no `firstPartyDomain` errors in the background console; marker cookie set with an FPD                         | bug 1 pin; marker FPI specs; e2e fpi suite (real FPI profile: cleanup + marker with FPD)                                                                                                                               |     |     |
| 6   | FPI leftovers                  | With FPI on, collect cookies; flip FPI off, restart, trigger cleanup                                                           | Old isolated cookies still get cleaned                                                                                       | bug 3 pin                                                                                                                                                                                                              |     |     |
| 7   | Containers: create/use         | Create a container, visit the test site in it, whitelist it from the popup (container setting ON)                              | Popup shows the container badge; the rule lands in the container list; cleanup honors it only in that container              | popup + list-mapping specs                                                                                                                                                                                             |     |     |
| 8   | Containers: default-list mode  | Container setting OFF, visit site in a container, whitelist from popup                                                         | Rule lands in the default list and protects the container tab's cookies                                                      | effectiveListKey specs                                                                                                                                                                                                 |     |     |
| 9   | Containers: delete             | With autoremove ON, delete a container that has cookies + a list                                                               | Its expression list disappears and its cookies are cleaned (verify via about:preferences cookie manager)                     | bug 6b spec                                                                                                                                                                                                            |     |     |
| 10  | Containers disabled pref       | `privacy.userContext.enabled=false`; restart; run a cleanup and open settings                                                  | No errors; cleanup covers default/private; selector shows no containers; orphaned lists still manageable                     | rejection-path specs                                                                                                                                                                                                   |     |     |
| 11  | Private windows, access ON     | Allow private-window access at install prompt; set a private-window whitelist from the popup; close/reopen private tabs        | Private cookies of whitelisted sites survive; others are cleaned; private domains never appear in the activity log           | bug 4 pin; scrub spec                                                                                                                                                                                                  |     |     |
| 12  | Private windows, access OFF    | Revoke private-window access in about:addons                                                                                   | No private cleanup, no errors; normal windows unaffected                                                                     | enumeration guard (incognito-access seed)                                                                                                                                                                              |     |     |
| 13  | Site-data cleanup              | With localStorage/indexedDB types on, collect storage on a subdomain, close tab                                                | Storage gone for the exact subdomain (check devtools); no `origins` errors in console; cache setting absent from settings UI | bug 5 pin; hostnames specs; e2e row 13 (wipe issued + logged; note: Gecko skips hostname-scoped storage removal for port-carrying lab origins like localhost:PORT, so the end state needs this manual real-domain row) |     |     |
| 14  | Cross-container storage guard  | Whitelist the site in container A; let its cookies qualify for cleanup in default                                              | Site storage survives (container A's protection vetoes the wipe)                                                             | guard specs                                                                                                                                                                                                            |     |     |
| 15  | Permission revocation          | Revoke site access in about:addons while the extension runs                                                                    | Red `!` badge appears; popup/settings show the banner; Restore access re-prompts and clears both                             | guard + banner specs                                                                                                                                                                                                   |     |     |
| 16  | Event-page restart             | Trigger cleanup activity, idle >30s (or kill the event page from about:debugging), interact again                              | Container names, pending cleanups, and tab→domain tracking survive the restart                                               | storage.session hydrate specs                                                                                                                                                                                          |     |     |
| 17  | Import old CAD export          | Import a Cookie AutoDelete 3.8 expressions file in settings                                                                    | Containers land as their own lists, private entries under Private, nothing folded                                            | import fixture specs                                                                                                                                                                                                   |     |     |
| 18  | Popup fit                      | Default settings, open popup on a busy site                                                                                    | No horizontal scroll or clipped controls at 430px; sections scroll internally                                                | sizing rationale (docs/firefox-ui-notes.md)                                                                                                                                                                            |     |     |

## Recording results

Put `OK`, `FAIL #NNN` (issue number), or `SKIP (reason)` in each channel cell, plus the run date and Firefox versions at the top of this section when a full pass completes.

### Runs

- _No full pass recorded yet. The branch must not merge to `main` until both channel columns are filled._
